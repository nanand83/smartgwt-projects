<DataSource	ID="Tab">
    <!-- NOTE: doesn't inherit from ImgTab, because multiple types of concrete tab
         implementations are possible - if you know you're using an ImgTab, you can
         specify <ImgTab> in your XML -->
	<fields>
		<field name="title"	type="string" basic="true" xmlAttribute="true"/>
		<field name="pane"	type="Canvas"/>
		<field name="ID"	type="identifier"    xmlAttribute="true"/>

        <!-- ID auto-generated by VB -->
        <field name="autoID" xmlAttribute="true" type="identifier" hidden="true"/>

		<field name="width"	type="measure" basic="true" xmlAttribute="true"/>

        <field name="disabled" type="boolean" basic="true"/>
        
        <field name="pickerTitle" type="string"/>
        
        <field name="icon" type="string" xmlAttribute="true" title="Icon URL"/>
        <field name="canClose" type="boolean" basic="true" xmlAttribute="true"/>
        <field name="closeIcon" type="string"/>
        
        <!-- backcompat -->
        <field name="enabled" type="boolean" visibility="hidden"/>
	</fields>
    <methods>
        <method title="Select Tab" name="select" action="true"/>
        <method name="tabSelected" />
        <method name="tabDeselected" />
    </methods>
</DataSource>
